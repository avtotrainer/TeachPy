# 📘 **ᲒᲐᲙᲕᲔᲗᲘᲚᲘ 01 — Python-ᲘᲡ ᲡᲐᲤᲣᲫᲕᲚᲔᲑᲘ**


# 1. ᲨᲔᲡᲐᲕᲐᲚᲘ — ᲠᲐᲡ ᲕᲐᲙᲔᲗᲔᲑᲗ ᲓᲐ ᲠᲐᲢᲝᲛ

Python არის ენა, რომლის მიზანია:  
**ადამიანმა შეძლოს რთული დავალებების ძალიან მარტივად აღწერა.**

ეს გაკვეთილი არის შენი პირველი ნაბიჯი პროგრამირების სამყაროში.  
დღეს ვისწავლით:

- რა არის პროგრამა  
- რა არის ცვლადი (პროგრამირების ყველაზე მნიშვნელოვანი ელემენტი)  
- რა განსხვავებაა რიცხვსა და ტექსტს შორის  
- როგორ “ველაპარაკებით” მომხმარებელს  
- დავწერთ პირველ პატარა პროგრამას  
- გავიხსენებთ, როგორ გამოიყენება იგივე პრინციპები შენს სამიზნე პროექტში  
  *(21-კარტიანი თამაში — `cart21.py`)*


---

# 2. ᲠᲐᲡ ᲜᲘᲨᲜᲐᲕᲡ “ᲞᲠᲝᲒᲠᲐᲛᲘᲠᲔᲑᲐ”?

კომპიუტერი შეგიძლია წარმოიდგინო მეგობრად, რომელიც ყველაფერს ზუსტად ისე აკეთებს, როგორც ეტყვი.  
მაგრამ თუ ოდნავ გაურკვევლად ეუბნები რამეს — ჩერდება და გეუბნება:  
„ვერ გავიგე, შენ მე შეცდომით მითხარი“.

საუბარი ყოველთვის ხდება რომელიმე ენაზე, ანალოგიურად კომპიუტერთან სასაუბროდ გამოიყნება პროგრამირების ენა.

აღნიშნული საუბარი ნიშნავს პროგრამირებას, ხოლო პროგრამირება თავისმხრივ ნიშნავს:

> **ნაბიჯ-ნაბიჯ ზუსტი ინსტრუქციების მიცემა კომპიუტერისთვის.**

პროგრამირების ენა ბევრია და ისინი ინსტრუქციებს წერილობით ფორმაში გამოხატავენ.

პროგრამირება პირველ რიგში გულისხმობს პროგრამის ტექსტის დაწერას.

Python არის ერთერთი პოპულარული პროგრამირების ენა, რომელის პროგრამული ტექსტებიც მაქსიმალურად ადვილი გასაგებია ადამიანისათვის. 


---

# 3. ᲙᲝᲛᲔᲜᲢᲐᲠᲘ - ᲣᲛᲜᲘᲨᲕᲜᲔᲚᲝᲕᲐᲜᲔᲡᲘ ᲐᲢᲠᲘᲑᲣᲢᲘ ᲞᲠᲝᲒᲠᲐᲛᲘᲡ ᲬᲔᲠᲘᲡᲐᲡ

კომენტარი პროგრამაში **აუცილებელი ატრიბუტია** — განსაკუთრებით მაშინ, როცა კოდი წლები უნდა გემსახუროს.

### **რატომაა კომენტარი მნიშვნელოვანი**

1. **ხსნის, რას აკეთებს კოდი**
   კოდი შეიძლება სწორი იყოს, მაგრამ გაუგებარი. კომენტარი განმართავს, თუ *„რა ხდება“*.

2. **იცავს ცოდნას დროში**
   დღეს ყველაფერი გახსოვს, მაგრამ ერთ თვეში?
   კომენტარი გიბრუნებს კონტექსტს.

3. **ამარტივებს კოლაბორაციას**
   კომენტარი არის გუნდური მუშაობის საერთო ენა.

4. **ასწავლის სწორ ჩვევას**
   დამწყებისათვის კომენტარების წერის ტრადიცია ნიშნავს მომავალში „წესრიგიანი“ კოდის წერას.

5. **მარტივად პოულობ შეცდომებს**
   როცა იციან, რა უნდა ხდებოდეს, კოდის არასწორი ადგილი მალე იძებნება.

---

### **კომენტარის სწორი ფორმა Python-ში**

```python
# ეს ბლოკი ამოწმებს ასაკობრივ შეზღუდვას
age = 12
if age >= 10:
    print("შენ შეგიძლია ეს თამაში ითამაშო")
```

---

### **რა შემთხვევაში უნდა გამოიყენო კომენტარი**

* როდესაც კოდი მიმართავს რთულ იდეას
* როდესაც რიცხვს ან სტრინგს აქვს **სპეციფიკური აზრი**
* როცა ფუნქციის დანიშნულება არ ჩანს ერთი შეხედვით
* როცა აკეთებ დროებით ჩანაწერს „მიყვანე აქამდე“

---

### **რა არ უნდა დაწერო კომენტარად**

* **რაც კოდიდან ისედაც აშკარაა**

ცუდი კომენტარის მაგალითი:

```python
x = 5  # აქ x-ს ვანიჭებთ 5-ს
```

---

### **შემაჯამებელი აზრი**

კომენტარი არის **პროგრამისტის პასუხისმგებლობა მომავალზე** — საკუთარ თავზე, სხვებზე და მთლიან პროექტზე. სწორად დაწერილი კომენტარი ზოგავს დროს, ამცირებს შეცდომებს და კოდს აძლევს კულტურულ, ტრადიციულ სტრუქტურას.


# 4. ᲪᲕᲚᲐᲓᲘ — ᲞᲘᲠᲕᲔᲚᲘ ᲓᲐ ᲧᲕᲔᲚᲐᲖᲔ ᲛᲜᲘᲨᲕᲜᲔᲚᲝᲕᲐᲜᲘ ᲘᲜᲡᲢᲠᲣᲛᲔᲜᲢᲘ

ცვლადი წარმოადგენს **სახელით მონათლულ ყუთს**, რომელშიც მონაცემს ვინახავთ.

ანალოგია რეალური ცხოვრებიდან:

- გაქვს ყუთი სახელად „ყულაბა“ — ვინახავთ ფულს.  
- გაქვს ყუთი „სათადარიგო გასაღები“ — შიგნით გასაღებია  
- გაქვს ყუთი „სურათები“ — შიგნით ფოტოებია

ყველა ამ ყუთს აქვს საერთო:
- **სახელი**  
- **შიგთავსი**

ცვლადის სახელის ჩაწერა პროგრამირებაში **ორ ნაწილად** განიხილება:

1. **სინტაქსური წესი** — რასაც ენა მოითხოვს სავალდებულოდ
2. **სტანდარტული შეთანხმება (შეფუთული ტრადიცია)** — რასაც პროგრამისტები თავად იცავენ, რომ კოდი იყოს მკაფიო, აკურატული და ერთგვაროვანი


---

## **1. ᲡᲘᲜᲢᲐᲥᲡᲣᲠᲘ ᲬᲔᲡᲘ (ᲡᲐᲕᲐᲚᲓᲔᲑᲣᲚᲝ ᲛᲝᲗᲮᲝᲕᲜᲔᲑᲘ Python-ᲨᲘ)**

Python-ში ცვლადის სახელი **უნდა** აკმაყოფილებდეს:

### ✔ მხოლოდ ასოები, ციფრები და ქვედატირე (`_`)

მაგ:

```
name
age2
user_name
```

### ✔ არ იწყება ციფრით

❌ `2name`
✔ `name2`

### ✔ დიდი და პატარა ასოები განსხვავდება

`age`, `Age`, `AGE` — სამივე სხვადასხვა ცვლადია.

### ✔ არ შეიძლება Python-ის "ქივორდი"

მაგ: `if`, `else`, `for`, `class`, `def` და სხვა.

### ✔ არ შეიძლება სპეციალური სიმბოლოები

❌ `name!`
❌ `my-name`
❌ `price$`

---

## **2. ᲞᲠᲝᲒᲠᲐᲛᲘᲡᲢᲔᲑᲘᲡ ᲛᲘᲔᲠ ᲛᲘᲦᲔᲑᲣᲚᲘ ᲢᲠᲐᲓᲘᲪᲘᲣᲚᲘ ᲨᲔᲗᲐᲜᲮᲛᲔᲑᲐ**

ეს არ არის კანონი, მაგრამ არის **წერილობითი კულტურა**, რომელსაც ყველა იცავს.

## **2.1 snake_case — Python-ის სტანდარტი**

ყველა სიტყვა პატარა ასოებით + ქვედატირე:

```
user_name
start_time
max_speed
student_age
```

ეს არის Python-ის ოფიციალური სტილი (PEP8).
პედაგოგიკის მხრივაც ყველაზე ნათელი ფორმაა.

---

## **2.2 ცვლადის სახელი უნდა აღწერდეს მის მნიშვნელობას**

ცუდი მაგალითი:

```
x = 12
a = "ანა"
```

სწორი:

```
age = 12
student_name = "ანა"
lesson_count = 7
```

კარგი პროგრამისტი **არ იტოვებს "გამოცანებით" წერის სტილს**.

---

## **2.3 camelCase — ნაკლებად გამოიყენება Python-ში**

თუმცა საჭიროა იცოდე.

```
userName
startTime
maxSpeed
```

ეს სტილი უფრო JavaScript-შია გავრცელებული. Python-ში **არ არის რეკომენდებული**, მაგრამ ცოდნა საჭიროა.

---

## **2.4 CONSTANT_CASE — მხოლოდ მუდმივებისათვის**

```
PI = 3.14
MAX_USERS = 50
DEFAULT_COLOR = "RED"
```

---

## **2.5 ტრადიციული სახელდების წესები**

### ✔ ცვლადის სახელი *არ უნდა იყოს ძალიან გრძელი*

```
student_current_daily_maximum_calculated_speed   ❌
```

### ✔ მაგრამ არც ძალიან მოკლე

```
s = 10  ❌
```

### ✔ ერთი შეხედვით უნდა მიხვდე, რას ნიშნავს

```
total_price
login_failed
is_student
has_access
```

### ✔ Boolean ცვლადები იწყება is/has/need …

```
is_ready = True
has_permission = False
needs_update = True
```

---

## **3. ᲠᲐᲢᲝᲛ ᲐᲠᲘᲡ ᲔᲡ ᲐᲡᲔᲗᲘ ᲛᲜᲘᲨᲕᲜᲔᲚᲝᲕᲐᲜᲘ**

* კოდის მკაფიო სტილი ბავშვებს უყალიბებს სწორი ჩვევას
* გუნდური მუშაობა მარტივდება
* შეცდომების პოვნა უადვილდება
* მომავალში Python-ის სტანდარტულ კოდთან სწრაფად შეეჩვევიან

---

# **მოკლე შეჯამება:**

**Python → snake_case.
სახელი — გასაგები.
არ იწყება ციფრით.
არ იყენებ ქივორდს.
არ წერ ბანძი მოკლე სახელებით.
Boolean იწყება is/has/need.
მუდმივებს → დიდი ასოებით.**


Python-ის ცვლადი სინამდვილეში არის **ბმული მეხსიერების ნაწილზე**, რომელსაც ჩვენთვის მოხერხებულ სახელად ჰქვია.


---

# 4. მონაცემთა ტიპები — რატომაა მნიშვნელოვანი?

Python-ს უნდა იცოდეს **რა ტიპის მონაცემს ჰქონდა ადგილი**, რათა გადაწყვიტოს როგორ მოიქცეს.

## 4.1 მთელი რიცხვი — `int`

```python
points = 10
```

გამოიყენება:

* შედარებაში
* არითმეტიკაში
* დათვლაში

## 4.2 ათწილადი — `float`

```python
price = 3.14
```

## 4.3 ტექსტი — `str`

```python
city = "Tbilisi"
```

ტექსტისთვის აუცილებელია ბრჭყალები!

⚠ `"25"` არის ტექსტი
⚠ `25` არის რიცხვი
**ეს ორი ერთმანეთს არ უდრის!**

შესაძლელია `'` - ერთმაგი ბრჭყალის გამოყნებაც:


⚠ `'25'` არის ტექსტი

თვითონ ბრჭყალების ბეჭდაზე გამოსატანად გამოიყენება ორმაგი ბრჭყალი, ბოლო ორმაგის გამოსატანად უბრალო ბრჭყალი:

```python
name = "'John'" # სტრიქონი 'John'
last_name = '"Doe"' # სტრიქონი "Doe"
```


## 4.4 ლოგიკური მონაცემი — `bool`

```python
logged_in = True
adult = False
```

ეს არის Python-ის „დიახ“ და „არა“. ამ ტიპის ცვლადით შეუძლია გვითხრას პითონმა, პირობა შესრულდება თუ არა. ლოგიკურ ცვლადზეა დამოკიდებული პპროგრამის მთლიანი ლოგიკა.

---

# 5. input() და print() — ᲠᲝᲒᲝᲠ ᲕᲔᲡᲐᲣᲑᲠᲝᲗ ᲛᲝᲛᲮᲛᲐᲠᲔᲑᲔᲚᲡ

პროგრამის უმეტესობა სწორედ ამ ორს იყენებს:

### print() — ᲢᲔᲥᲡᲢᲘᲡ ᲒᲐᲛᲝᲢᲐᲜᲐ

```python
print("გამარჯობა!")
```

### input() — ᲛᲝᲛᲮᲛᲐᲠᲔᲑᲚᲘᲡᲒᲐᲜ ᲢᲔᲥᲡᲢᲘᲡ ᲛᲘᲦᲔᲑᲐ

```python
name = input("რა გქვია? ")
```

ამ ბრძანებით პროგრამის მიმდინარეობა ჩერდება:

1. ეკრანზე გამოიტანს ტექსტს
2. დაელოდება შეტანას
3. მიღებულ ტექსტს შეინახავს `name` ცვლადში

⚠ input() ყოველთვის აბრუნებს TEXT-ს.
ანუ თუ შეკითხვაზე პასუხს გავცემთ 25, ის კომპიუტერში შევა, როგორც `"25"` — სტრიქონი.

თუ რიცხვი გვჭირდება, მაშინ შეყვანილი რიცხვი ცხადად უნდა გარდავქმნათ მთელ რიცხვად და ამისათვის უნდა გამოვიყენოთ ფუნქცია `int()`.:

```python
age = int(input("რამდენი წლის ხარ? "))
```
ეს კოდი შეიძლება დაიწეროს ასეც:

```python
age = input("რამდენი წლის ხარ? ")
age = int(age)
```
აქ `age` ცვლადში ჯერ ვინახავთ შემოტანილ რიცხვის ტექსტურ ვარიანტს, ხოლო შემდეგ მას ვაკონვერტებთ მთელ რიცხვში.

შენიშვნა: კოდის ორივე მაგალითიი შეიცავს შეცდომის წარმოშობის შესაძლებლობას, იმ შემთხვევაში, თუ მომხმარებელი არ შეიყვანს რიცხვს, არამედ რაიმე ტექსტს, მაშინ `int()` ფუნქცია შეეცდება ტექსტის რიცხვად გარდაქმნას, რაც გამოიწვევს შეცდომას.

```python
age = int('Twelve')  # გამოიწვევს შეცდომას

```

*ამ შეცდომის გამორიცხვა შესაძლებელია და გავაკეთებთ როცა ვისწავლით შედარების ოპერატორს.*

---

# 6. ᲞᲘᲠᲕᲔᲚᲘ ᲞᲠᲝᲒᲠᲐᲛᲐ — ᲓᲐᲬᲔᲠᲔ ᲓᲐ ᲒᲐᲘᲐᲖᲠᲔ

დავწეროთ პირველი **„ინტერაქტიული“** პროგრამა:

```python
name = input("შეიყვანე შენი სახელი: ")
age = int(input("და აკეთებ რამდენ წელს? "))

print("სასიამოვნოა შენი გაცნობა,", name)
print("შენი ასაკია:", age)
```

### ᲠᲝᲒᲝᲠ ᲛᲣᲨᲐᲝᲑᲡ ᲔᲡ?

1. პროგრამა გთხოვს სახელს
2. შემოგაქვს ცვლადი `name`, რომელშიც ინახავ კლავიატურიდან შეყვანილ ტექსტს
3. შემდეგ გეკითხება ასაკს და კლავიატურიდან უნდა შევიყვანოთ რიცხვი 
4. `int()` გარდაქმნის ტექსტ-რიცხვს კომპიუტერის მთელ რიცხვად
5. ბოლოს ორივე მონაცემი გამოაქვს print()-ს

ეს არის პროგრამირების „სტარტის სტარტი“.

---


# 8. ᲒᲐᲕᲠᲪᲔᲚᲔᲑᲣᲚᲘ ᲨᲔᲪᲓᲝᲛᲔᲑᲘ ᲓᲐᲛᲬᲧᲔᲑᲔᲑᲨᲘ

### ❌ შეცდომა: ცვლადის სახელის დაწყება ციფრით

```python
1name = "Gio"   # არ შეიძლება
```

### ❌ ბრჭყალების დავიწყება ტექსტში

```python
city = Tbilisi   # შეცდომაა
```

### ❌ input()–ის რიცხვად არგადაქცევა

```python
age = input("age: ")  # აქ age არის ტექსტი, რიცხვი არა
```

მაგრამ სწორი ვერსიაა:

```python
age = int(input("age: "))
```

---

# 9. ᲤᲝᲠᲛᲐᲢᲘᲠᲔᲑᲣᲚᲘ ᲡᲢᲠᲘᲥᲝᲜᲘ - **f-ᲡᲢᲠᲘᲥᲝᲜᲘ** 

ეს არის სტრიქონი, რომლის შიგნით ფიგურული ფრჩხილების `{}` გამოყენებით შეგიძლია ჩასვა Python-ის კოდი (გამოსახულება), რომელიც გამოითვლება და ავტომატურად ჩაჯდება ტექსტში.

---

##  ᲡᲘᲜᲢᲐᲥᲡᲘ

ძირითადი წესი:

```python
name = "ავტო"
age = 12

text = f"მე მქვია {name} და ვარ {age} წლის."
print(text)
```

შედეგი:

```text
მე მქვია ავტო და ვარ 12 წლის.
```

**მთავარი წესები:**

* სტრიქონის წინ იწერება პატარა ან დიდი ასო: `f` ან `F`
* შიგნით `{ ... }` ფრჩხილებში წერ:

  * ცვლადის სახელს
  * ან ნებისმიერ გამოსახულებას (expression)

---

## ᲠᲐ ᲨᲔᲘᲫᲚᲔᲑᲐ ᲩᲐᲬᲔᲠᲝ `{ }`-ᲘᲡ ᲨᲘᲒᲜᲘᲗ

ყველანაირი გამოსახულება:

```python
a = 5
b = 7

print(f"a + b = {a + b}")
print(f"a * b = {a * b}")
print(f"საშუალო = {(a + b) / 2}")
```

შედეგი:

```text
a + b = 12
a * b = 35
საშუალო = 6.0
```


---

##  ᲤᲝᲠᲛᲐᲢᲘᲠᲔᲑᲐ (ᲡᲘᲒᲠᲫᲔ, ᲜᲘᲨᲜᲔᲑᲘ, ᲐᲗᲬᲘᲚᲐᲓᲘ ᲓᲐ Ა.Შ.)

ფორმატი იწერება ორწერტილის შემდეგ `:`:

```python
pi = 3.14159265

print(f"pi ორი ნიშნით: {pi:.2f}")       # 3.14
print(f"pi ოთხი ნიშნით: {pi:.4f}")      # 3.1416
number = 7
print(f"მარცხნივ გაწევა: {number:5}")   # '    7'
print(f"ნულებით შევსება: {number:05}")  # '00007'
```

ფორმატის საერთო სტრუქტურა: `{value:format_spec}`

---



##  ᲨᲔᲓᲐᲠᲔᲑᲐ ᲫᲕᲔᲚ  ᲛᲔᲗᲝᲓᲔᲑᲗᲐᲜ

ძველი ვარიანტები:

```python
name = "ავტო"
age = 12

# %-ით
print("მე მქვია %s და ვარ %d წლის." % (name, age))

# format()-ით
print("მე მქვია {} და ვარ {} წლის.".format(name, age))

# f-string
print(f"მე მქვია {name} და ვარ {age} წლის.")
```

**f-სტრიქონი** უფრო მოკლეა და კითხვისთვის უფრო გასაგები.

---


### ᲛᲠᲐᲕᲐᲚᲡᲢᲠᲘᲥᲝᲜᲘᲐᲜᲘ f-ᲡᲢᲠᲘᲥᲝᲜᲘ

```python
name = "ავტო"
age = 12

text = f"""
მოსწავლის მონაცემები:
სახელი: {name}
ასაკი: {age}
"""
print(text)
```

---

**ᲛᲝᲙᲚᲔ ᲨᲔᲯᲐᲛᲔᲑᲐ:**

* `f""` ან `f''` — სტრიქონი, სადაც `{}`-ის შიგნით წერ კოდს.
* `{}` შიგნით შეიძლება იყოს ცვლადი ან ნებისმიერი გამოსახულება.
* ფორმატირებისთვის გამოიყენება `:` — `{x:.2f}`, `{n:05}` და ა.შ.
* ფიგურული ფრჩხილები ტექსტში — `{{` და `}}`.

აქ მოყვანილი სტრიქონის ფორმატირება შეესაბამება ახლანდელ ცოდნის დონეს, თუმცა მას კიდევ უფრო მდიდარი შესაძლებლობები აქვს, რომლებსაც დავუბრუნდებით შემდეგ თავებშიც.

# 10. ᲡᲮᲕᲐᲓᲐᲡᲮᲕᲐ ᲡᲘᲫᲜᲔᲚᲘᲡ ᲞᲐᲢᲐᲠᲐ ᲡᲐᲕᲐᲠᲯᲘᲨᲝᲔᲑᲘ 

ამ დავალებების შესრულება უნდა სცადო დამოუკიდებლად

## Level A — ᲛᲐᲠᲢᲘᲕᲘ

1. პროგრამა მომხმარებელს ეკითხება სახელს და უწერს: „გამარჯობა <სახელი>“
2. პროგრამა ეკითხება ქალაქს და წერს: „შენ ცხოვრობ ქალაქში <ქალაქი>“.

## Level B — ᲡᲐᲨᲣᲐᲚᲝ

3. მომხმარებელს ჰკითხე: სახელი, გვარი, ასაკი — და დაბეჭდე ერთ სტრიქონში.
4. მიიღე ორი რიცხვი და დაბეჭდე მათი ჯამი.

## Level C — ᲠᲗᲣᲚᲘ

5. ჰკითხე დაბადების წელი, გადაყვანე int-ად და გამოთვალე რეალური ასაკი (2025–იდან გამოაკელი).
6. დაწერე პროგრამა, რომელიც იღებს 3 input-ს (სახელი, ასაკი, ქვეყანა) და ბეჭდავს მოკლე „პირად ბარათს“.

---

# 10. ᲨᲔᲯᲐᲛᲔᲑᲐ

ამ გაკვეთილიში:

* ვისწავლეთ ცვლადები
* გავიგეთ მონაცემთა ტიპები
* ვისწავლეთ input()/print()
* დავწერეთ პირველი ინტერაქტიული პროგრამა
* მოკლედ f-სტრიქონი
* შევასრულეთ სავარჯიშოები 3 დონის მიხედვით


