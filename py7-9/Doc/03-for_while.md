მზადაა — ახლა მოგცემ **გაკვეთილი 03**–ს იმავე სტილში, დიდი, დეტალური, ხარისხიანი, აუდიტორიისთვის იდეალურად მოსამზადებელი ჰენდაუტის სახით.

ეს გაკვეთილი ძალიან მნიშვნელოვანია, რადგან **ციკლები** Python-ის ყველა პროექტის ბაზაა,
მათ შორის თქვენს 21–კარტიან თამაშშიც, სადაც 3-ჯერ მეორდება ბარათების დალაგება.

👇 ყველაფერი ჩასმულია **კარგად დახურული კოდის ბლოკებით** — HTML კონვერტაცია არ მოხდება.

---

# 📘 **გაკვეთილი 03 — ციკლები (for, while), range(), iteration**

*(დეტალური, ნარატივული, გაკვეთილის ზომის ტექსტი)*

````md
# გაკვეთილი 03 — ციკლები: for, while, range()

---

# 1. შესავალი — რატომ გვჭირდება ციკლები?

წინა გაკვეთილში ვისწავლეთ, როგორ მივცემთ პროგრამას გადაწყვეტილებას (if).  
ახლა შევისწავლით **როგორ გავაკეთებინოთ პროგრამას ერთი და იგივე მოქმედება ბევრჯერ**.

ციკლი აუცილებელია მაშინ, როცა:

- უნდა დავბეჭდოთ 100 ელემენტი;
- უნდა ჩავატარო გამოთვლა 1000-ჯერ;
- უნდა დავთვალოთ სიაში არსებული ყველა ელემენტი;
- მომხმარებელს ვეკითხებით რამეს მანამ, სანამ სწორ პასუხს არ მოგვცემს;
- თამაშში ვიმეორებთ ნაბიჯს **3-ჯერ**, როგორც 21-კარტიან მაგიაში.

ციკლი პროგრამირებაში არის სიტყვა:

> „გამიკეთე ეს ისევ და ისევ, მანამ სანამ აღარ დაგჭირდება“.

---

# 2. რა არის 반복ი?

წარმოიდგინე მასწავლებელი, რომელიც გეუბნება:
“10-ჯერ დაჯექი და წამოდექი”.

მან არ თქვა:
- „დაჯექი“
- „წამოდექი“
- „დაჯექი“
- „წამოდექი“ …
(ასწაული სიგრძის ინსტრუქცია)

მან თქვა:
> **გაიმეორე ეს მოქმედება 10-ჯერ.**

Python დროს ზუსტად ესისწავლება ამ გაკვეთილში.

---

# 3. for ციკლი — როცა ვიცით რამდენჯერ უნდა გავიმეოროთ

`for` ციკლი არის „დავლდივით“ მექანიზმი:

```python
for i in range(5):
    print("გამეორება:", i)
````

### რა ხდება აქ?

* `range(5)` ქმნის რიცხვებს: 0, 1, 2, 3, 4
* `for i in range(5)` ნიშნავს:
  „ყოველ რიცხვზე, რაც `range(5)`–შია, შეასრულე ჩემი კოდი“

ასე რომ:

* 5 გამეორება
* ცვლადი `i` იღებს მნიშვნელობებს 0 → 4
* ციკლი მთავრდება, როცა ელემენტები იწურება

### რატომ იწყება 0-დან?

Python-ში მრავალი სტრუქტურა 0–ზეა დამყარებული
(ასევე სიები, ინდექსები, range — ეს საერთოა პროგრამირებაში).

---

# 4. range() — ციკლების საწვავი

`range()` ფუნქცია აძლევს ციკლს რიცხვების მიმდევრობას.

### 1 არგუმენტით:

```python
range(5)  # 0,1,2,3,4
```

### 2 არგუმენტით:

```python
range(2, 7)  # 2,3,4,5,6
```

### 3 არგუმენტით (ნაბიჯი):

```python
range(1, 10, 2)  # 1,3,5,7,9
```

### უკუღმა თვლა:

```python
range(10, 0, -1)  # 10,9,8,...,1
```

range() არ ქმნის ნამდვილ სიას — ეს არის **ობიექტი**, რომელიც რიცხვებს საჭიროებისამებრ ქმნის.
(ეს ეფექტურობისთვისაა — RAM-ს არ ხარჯავს ზედმეტად.)

---

# 5. while ციკლი — როცა არ ვიცით რამდენი გამეორება იქნება

if იკითხავს:

> „მართალია თუ არა?“

while ამბობს:

> „მანამ გაიმეორე, სანამ ეს პირობა მართალია“.

მაგალითად:

```python
x = 0

while x < 5:
    print("x არის:", x)
    x += 1
```

### ახსნა:

* სანამ `x < 5` — True
* print() შესრულდება
* ბოლოს `x += 1` ზრდის მნიშვნელობას
* 5-ზე რომ ავა — ციკლი შეწყდება

⚠ თუ პირობა არასდროს გახდება False → მიიღებ უსასრულო ციკლს.

---

# 6. break და continue — ციკლის კონტროლი

### break — ციკლის გაწყვეტა

```python
for i in range(10):
    if i == 5:
        break
    print(i)
```

### continue — გამოტოვება და გაგრძელება

```python
for i in range(6):
    if i == 3:
        continue
    print(i)
```

---

# 7. iteration — როგორ მუშაობს for-ის შიგნით?

`for` ციკლი მუშაობს „იტერატორთან“ — ობიექტთან, რომელსაც შეგვიძლია თითოეულ ელემენტად გადავუყვეთ.

მაგალითად:

```python
for letter in "HELLO":
    print(letter)
```

შედეგი:
H
E
L
L
O

სტრიქონი ითვლება iterable ობიექტად.

ასევე iterable-ია:

* list
* tuple
* set
* dict (გასაღებები)

---

# 8. 21–კარტიანი პროექტი — სად იყენებ for ციკლს?

გახსენებ მცირე ფრაგმენტს (cart21.py) 

```python
for i in range(1, 4):
    print(f"--- ROUND {i} ---")
```

ეს ციკლი:

* 3-ჯერ იმეორებს ბარათების დაწყობას
* თითო რაუნდში თამაშის ლოგიკა მეორდება
* იდენტიკურად ხდება ბარათების გადანაწილება და player–ის ინტერაქცია

ეს არის თამაშის „გული“ —
3 გამეორება = 21 ბარათის მაგიის საიდუმლო.

შემდეგ, როცა თამაშს მოდულებად გადატანას გავაკეთებთ, კარგად დაინახავ რამდენად მნიშვნელოვანია iteration.

---

# 9. რთული iteration — სიის ელემენტებზე გავლა

```python
nums = [10, 20, 30, 40]

for n in nums:
    print(n)
```

---

# 10. enumerate() — როცა გვჭირდება ინდექსიც

```python
for index, value in enumerate(["a","b","c"]):
    print(index, value)
```

შედეგი:
0 a
1 b
2 c

---

# 11. while + input — მომხმარებლის შეკითხვების ციკლი

```python
while True:
    cmd = input("შეიყვანე ბრძანება: ")
    if cmd == "exit":
        break
    print("შენ შეიყვანე:", cmd)
```

ეს არის CLI აპლიკაციის საფუძველი.

---

# 12. დიდი პრაქტიკული სავარჯიშოები

## Level A — მარტივი

1. დაბეჭდე რიცხვები 1-დან 10-მდე (for).
2. while ციკლით დაითვალე 5-დან 0-მდე.
3. დაბეჭდე სიტყვა „Python“ 7-ჯერ.

## Level B — საშუალო

4. სიის ელემენტების ჯამის გამოთვლა for ციკლით.
5. დაბეჭდე მხოლოდ ლუწი რიცხვები range-ის დახმარებით.
6. while ციკლით შეიყვანე რიცხვები მანამ, სანამ მომხმარებელი 0-ს არ შეიყვანს.

## Level C — რთული

7. დაწერე პროგრამა, რომელიც იპოვის სიის უდიდეს ელემენტს for iteration-ით (max()–ის გარეშე).
8. შექმენი mini-login ლუპი:
   პროგრამა მომხმარებელს 3 მცდელობას აძლევს პაროლის შესაყვანად.
   თუ 3-ჯერ არასწორია → „ბლოკირება“.
9. შექმენი script, რომელიც სტრიქონში ითვლის ხმოვნებსა და თანხმოვნებს for iteration–ით.

---

# 13. შეჯამება — რა ვისწავლეთ?

* iteration (გამეორება) არის programming-ის ერთ-ერთი მთავარი საფუძველი
* for — როცა ვიცით რამდენჯერ
* while — როცა პირობა მართალია
* range() — საიდან მოვიდა loop-ის რიცხვები
* break/continue — კონტროლი
* iteration მუშაობს ყველა iterable ობიექტზე
* 21-კარტიან პროექტში for ციკლები მართავენ თამაშის 3 რაუნდს

შემდეგ გაკვეთილში შევალთ Python-ის **კონტეინერების სამყაროში**:

👉 list
👉 dict
👉 tuple
👉 set

და ისინიც iteration-ში გამოყენებას საჭიროებენ.

```

---

# ✔ მზად ვარ გაგრძელებაზე  
თუ გინდა, მომიწერე:

**„გაკვეთილი 04“**

და მოგიტან იგივე დონის, დიდი და დეტალური გაკვეთილს კოლექციებზე.
```

